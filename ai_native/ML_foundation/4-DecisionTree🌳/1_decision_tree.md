Một con số đo mức độ hỗn loạn.

- > Entropy ra đời.

Ví dụ: YES YES YES NO
🤔
Entropy nên là bao nhiêu?
0.2?
0.3?
0.7?
0.9?

"Làm sao biến trực giác thành một con số nhất quán?"

            H=−∑pi​log2​(pi​) ( Cơ số 2) Hay −∑plog(p) 

            Entropy= Σ(probability) × (log(probability))

            <!-- Có Binary Cross Entropy −(ylog(p)+(1−y)log(1−p)) -->

Log rất giỏi trong việc đo "mức độ bất ngờ" (surprise) hoặc "độ không chắc chắn"

